// src/tests/msw/handlers/index.ts
// Import handler arrays from each API domain file and spread them here.
// Example: import { uploadHandlers } from './upload';
//          export const handlers = [...uploadHandlers];
// Start empty — add handlers as the API surface grows.

export const handlers: never[] = [];
